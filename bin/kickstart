#!/usr/bin/env node

'use strict';

var path = require('path');
var fs = require('fs');
var kickstart = require('../lib/kickstart');

kickstart.startproject()
  .then(function(){
      process.exit(0);
  })
  .catch(function(err){
      console.log(err);
      process.exit(1);
  })
  .done();
